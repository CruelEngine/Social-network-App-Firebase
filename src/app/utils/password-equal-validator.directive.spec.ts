import { Passwordequalvalidator } from './password-equal-validator.directive';

describe('Passwordequalvalidator', () => {
  it('should create an instance', () => {
    const directive = new Passwordequalvalidator();
    expect(directive).toBeTruthy();
  });
});
